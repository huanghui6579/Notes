<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    
    <SwitchPreference 
        android:key="@string/settings_key_sync_note_auto"
        android:title="@string/settings_sync_note_title"
        android:summary="@string/settings_sync_note_summary"
        android:defaultValue="@bool/default_settings_key_sync_note_auto"
        android:checked="@bool/default_settings_key_sync_note_auto"/>
    
    <SwitchPreference
        android:key="@string/settings_key_sync_note_traffic"
        android:title="@string/settings_sync_traffic_title"
        android:summary="@string/settings_sync_traffic_summary"
        android:defaultValue="@bool/default_settings_key_sync_note_traffic"
        android:checked="@bool/default_settings_key_sync_note_traffic"/>
    
    <Preference
        android:key="@string/settings_key_sync_note_state"
        android:title="@string/settings_sync_state_title"
        android:summary="@string/settings_sync_state_summary" />
    
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference
        android:defaultValue="180"
        android:entries="@array/pref_sync_frequency_titles"
        android:entryValues="@array/pref_sync_frequency_values"
        android:key="sync_frequency"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_sync_frequency" />-->

    <!-- This preference simply launches an intent when selected. Use this UI sparingly, per
         design guidelines. -->
    <!--<Preference android:title="@string/pref_title_system_sync_settings">
        <intent android:action="android.settings.SYNC_SETTINGS" />
    </Preference>-->

</PreferenceScreen>
